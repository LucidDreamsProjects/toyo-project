/* const getUserProfile = async (): Promise<void | Profile> => {
  const profile = await arkaneConnect.api
    .getProfile()
    .then((profile) => {
      console.log(profile);
      return profile;
    })
    .catch((error) => {
      console.error(error);
    });

  return profile;
};
 */
