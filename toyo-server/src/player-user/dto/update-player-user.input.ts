// import { CreatePlayerUserInput } from './create-player-user.input';

export class UpdatePlayerUserInput {
  userId!: number;
  wallet!: string;
  username!: string;
  tag!: number;
  name!: string;
  icon!: number;
  address!: string;
  replays!: string;
  role!: number;
}
